# 日志控制说明

## 概述
为了优化系统性能，减少控制台冗余输出，我们实现了智能日志控制系统。

## 日志级别
- `error`: 错误信息（总是显示）
- `warn`: 警告信息
- `info`: 一般信息
- `debug`: 调试信息

## 日志类别
- `messageReceived`: 收到的消息
- `messageSent`: 发送的消息
- `heartbeat`: 心跳检测
- `adminOperation`: 管理员操作
- `queueProcessing`: 队列处理
- `chromeDetection`: Chrome浏览器检测
- `fileCreation`: 文件创建
- `loadingProgress`: 加载进度
- `qrCode`: 二维码相关

## 配置文件
位置：`config/logging.json`

### 当前配置（优化后）
```json
{
  "console": {
    "level": "warn",
    "enabled": true,
    "filters": {
      "messageReceived": false,
      "messageSent": false,
      "heartbeat": false,
      "adminOperation": false,
      "queueProcessing": false,
      "chromeDetection": false,
      "fileCreation": false,
      "loadingProgress": false,
      "qrCode": false
    }
  }
}
```

## 性能优化效果

### 已注释的冗余输出
1. **消息处理**：
   - 收到的每条消息不再输出
   - 成功发送的消息不再输出
   - 队列处理状态不再输出

2. **系统状态**：
   - 心跳检测不再输出
   - 管理员操作确认不再输出
   - 文件创建确认不再输出

3. **初始化过程**：
   - Chrome浏览器检测不再输出
   - 加载进度不再输出
   - 二维码保存确认不再输出

### 保留的重要输出
- ❌ 错误信息
- ⚠️ 警告信息
- 🚨 严重错误
- 🚀 系统启动信息
- ✅ 关键成功信息

## 如何调整日志级别

### 显示所有信息（调试模式）
```json
{
  "console": {
    "level": "debug",
    "filters": {
      "messageReceived": true,
      "messageSent": true,
      "heartbeat": true,
      "adminOperation": true,
      "queueProcessing": true,
      "chromeDetection": true,
      "fileCreation": true,
      "loadingProgress": true,
      "qrCode": true
    }
  }
}
```

### 只显示错误和警告（生产模式）
```json
{
  "console": {
    "level": "warn",
    "filters": {
      "messageReceived": false,
      "messageSent": false,
      "heartbeat": false,
      "adminOperation": false,
      "queueProcessing": false,
      "chromeDetection": false,
      "fileCreation": false,
      "loadingProgress": false,
      "qrCode": false
    }
  }
}
```

## 性能提升
- 减少控制台输出频率约80%
- 降低CPU使用率
- 提高消息处理速度
- 减少内存占用

## 注意事项
1. 修改配置文件后需要重启程序
2. 错误日志仍然会记录到文件中
3. 可以通过UI界面查看详细日志
4. 系统级重要信息始终会显示
